{
  "scripts": {
    "build": "npx @tailwindcss/cli -i static/styles.css -o static/tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "tailwindcss": "^4.2.2"
  }
}
